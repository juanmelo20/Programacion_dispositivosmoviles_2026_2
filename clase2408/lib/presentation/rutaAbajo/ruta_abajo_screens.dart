import 'package:flutter/material.dart';

class RutaAbajoScreen extends StatelessWidget {
  const RutaAbajoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('hola abajo'),
      appBar: AppBar(
        title: Text('abajo '),
      )
      
    );
  }
}