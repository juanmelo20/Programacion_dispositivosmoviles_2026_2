import 'package:flutter/material.dart';

class RutaArribaScreens extends StatelessWidget {
  const RutaArribaScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('hola arriba'),
      appBar: AppBar(
        title: Text('arriba '),
      )
    );
  }
}